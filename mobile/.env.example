API_URL=http://10.0.2.2:3000/api/v1
WEBSOCKET_URL=http://10.0.2.2:3000

# 10.0.2.2 is the Android emulator's alias for the host's localhost.
# Use http://localhost:3000 for iOS simulator, or the LAN IP for physical devices.

# NO GOOGLE_MAPS_API_KEY — mobile app uses text-based navigation, no map tiles.
