<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  package="com.plowpath.driver">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
      android:name=".MainApplication"
      android:label="PlowPath Driver"
      android:allowBackup="false"
      android:theme="@style/AppTheme">
      
      <activity
        android:name=".MainActivity"
        android:label="PlowPath Driver"
        android:configChanges="keyboard|keyboardHidden|orientation|screenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity>

      <!-- @mauron85/react-native-background-geolocation auto-merges its own
           LocationServiceImpl manifest via gradle library merging. We only need
           to assert the foregroundServiceType so Android 14+ accepts the service.
           If the library's manifest stops being merged after a RN upgrade, the
           explicit declaration below can be uncommented:
           <service
             android:name="com.marianhello.bgloc.service.LocationServiceImpl"
             android:foregroundServiceType="location"
             android:exported="false" /> -->
    </application>
</manifest>
